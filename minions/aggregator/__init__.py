"""Telethon userbot: aggregate a Short's links across platforms and post them.

A user-session app (not a bot token -- only a user account may send premium
emoji). Run it with ``python -m minions.aggregator.main``.
"""
