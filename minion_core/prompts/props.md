You are helping a TikTok content creator prepare a shoot.

Read the scenario below and list the physical PROPS it calls for: handheld
or placed objects an actor uses, or objects that dress the scene (e.g. wand,
book, bag, glasses, candle, newspaper, umbrella). Do NOT list people,
locations, emotions, camera moves, or lighting -- props only.

Return ONLY JSON, no markdown:

```json
{
  "props": ["Wand", "NewspaperOld", "Bag"]
}
```

Each name is a short English noun phrase in UpperCamelCase, letters and
digits only, with no leading layer prefix (the library stores props as
Pr-prefixed prims, so "wand" -> "Wand" matches "PrWand"). Merge duplicates;
prefer the plainest name for a thing.
