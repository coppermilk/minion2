# Telethon session files hold the account auth key — as powerful as your
# password. NEVER commit them.
*.session
*.session-journal

# Local secrets.
.env

# Aggregator runtime state (in-flight video groups); regenerated at runtime.
aggregator_state.json
aggregator_state.tmp
